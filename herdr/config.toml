# Herdr configuration
#
# Home Manager deploys this file read-only. Add durable, cross-machine
# preferences here rather than through Herdr's in-app settings.
# Run `herdr --default-config` to view the full commented reference.

# The onboarding flow tries to persist this value itself, which cannot work
# with a read-only, declaratively managed configuration file.
onboarding = false
